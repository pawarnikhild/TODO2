import { StyleSheet } from 'react-native';
import { FontSize, AppColor } from '../../utils/StyleConstants';
export default StyleSheet.create({
    heading: {
        fontSize: FontSize.Header
    },
    CustomCardContainer: {
        margin: 4,
    },

    
});